# Vesper Configuration
# For local/personal use, these defaults work out of the box!

# Redis (Working Memory Cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# Qdrant (Vector Database)
QDRANT_URL=http://localhost:6333

# SQLite (Knowledge Graph)
SQLITE_DB=./data/memory.db

# Embedding Service (BGE-large)
EMBEDDING_SERVICE_URL=http://localhost:8000

# Application
NODE_ENV=development
LOG_LEVEL=info

# =============================================================================
# Quick Start
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Start services: docker-compose up -d
# 3. That's it! No passwords or API keys required for local use.
#
# The system will automatically create the data/ directory for SQLite.
