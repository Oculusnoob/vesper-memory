# Vesper Configuration
# For local/personal use, these defaults work out of the box!

# Redis (Working Memory Cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# Qdrant (Vector Database)
QDRANT_URL=http://localhost:6333

# SQLite (Knowledge Graph)
# Default: ~/.vesper/data/memory.db (user-level storage)
# Override with absolute path if needed
# SQLITE_DB=~/.vesper/data/memory.db

# Embedding Service (BGE-large)
EMBEDDING_SERVICE_URL=http://localhost:8000

# Application
NODE_ENV=development
LOG_LEVEL=info

# =============================================================================
# Quick Start
# =============================================================================
# 1. Install: npm install -g vesper-memory
# 2. Setup: vesper install
# 3. That's it! No passwords or API keys required for local use.
#
# Data is stored at ~/.vesper/ (user-level storage, persists across projects)
# - SQLite database: ~/.vesper/data/memory.db
# - Docker volumes: ~/.vesper/docker-data/
